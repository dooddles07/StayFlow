# Frontend is deployed to Vercel, which proxies /api/* to the Render API (see
# vercel.json) — so in production VITE_API_URL is unset and the default (/api) is
# correct. Only set this for local dev if running the backend on a different origin.
# VITE_API_URL=http://localhost:4000/api

# Backend vars. Needed by the Prisma CLI and the standalone backend dev server
# (server/: npm run dev). This is the ONLY .env in the repo — there is deliberately no
# server/.env. On Render these are set directly as service env vars, not via this file.
# See docs/SECURITY.md.
DATABASE_URL=postgresql://user:password@host:5432/db
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d

# Only needed by the standalone backend dev server (server/: npm run dev, port 4000)
# talking to a separately-run frontend dev server (vite dev --port 3000).
PORT=4000
CORS_ORIGIN=http://localhost:3000,https://your-frontend-domain.example

# production toggles secure cookies + the prod mailer path; unset is fine for local dev.
NODE_ENV=development

# Base URL used to build password-reset/email-change links. Falls back to the first
# CORS_ORIGIN entry, then http://localhost:3000, if unset.
APP_URL=http://localhost:3000

# Resend API key. Unset means password-reset/email-change links are only logged to the
# console instead of emailed — fine for local dev, required in production. See docs/SECURITY.md.
RESEND_API_KEY=
MAIL_FROM=StayFlow <onboarding@resend.dev>
