node_modules
.DS_Store
dist
dist-ssr
*.local
# Every .env variant, not just the bare name — .env.production and .env.staging
# hold the same secrets and were not covered before.
.env*
!.env.example
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.playwright-mcp
.claude_review_diff.txt
.impeccable
